import { StartButtonWrapper } from "../../styles/SHomePage";

const StartButton = () => {
    return(
        <StartButtonWrapper>
            <button>
                시작하기
            </button>
        </StartButtonWrapper>
    );
}

export default StartButton;