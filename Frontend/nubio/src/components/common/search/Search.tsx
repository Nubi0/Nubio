import { SearchWrapper } from "../../../styles/SSearch";
import SearchBar from "./SearchBar";

const Search = () => {
  return (
    <SearchWrapper>
      <SearchBar />
    </SearchWrapper>
  );
};

export default Search;
