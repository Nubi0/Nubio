type propsType = {
  searchKeyword: string;
};

type placeType = {
  place_name: string;
  road_address_name: string;
  address_name: string;
  phone: string;
  place_url: string;
  x: number;
  y: number;
  length: number;
};

type SetDirectionProps = {
  map: any;
};
