import styled from 'styled-components';

const SafeButton = () => {
    return(
        <SafeButtonWrapper>
            <button>
                안전
            </button>
        </SafeButtonWrapper>

    );
}

export default SafeButton;

const SafeButtonWrapper = styled.div``;