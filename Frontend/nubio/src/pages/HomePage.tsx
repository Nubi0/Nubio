import styled from 'styled-components';

const HomePage = () => {
    return (
        <HomePageWrapper>

        </HomePageWrapper>
    );
}

export default HomePage;

const HomePageWrapper = styled.div``;
